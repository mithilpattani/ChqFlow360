# ChqFlow360 Prototypes

Live demo (dummy branding): hosted via GitHub Pages from this repo.

Once GitHub Pages is enabled (Settings -> Pages -> Source: main / root),
the demo is reachable at the repo's Pages URL, e.g.:
https://mithilpattani.github.io/ChqFlow360/

`index.html` is the current version. Future versions will be added here
and index.html updated to point at the latest.
